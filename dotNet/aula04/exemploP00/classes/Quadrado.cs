using System;

namespace exemploP00
{
    class Quadrado
    {
        public double calcularArea(int lado)
        {   
            // Pow = potencia (x,2) elevado a dois
            return Math.Pow(lado,2);
        }
    }
}